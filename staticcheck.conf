# staticcheck configuration.
#
# Most stylecheck (ST10xx) checks are enabled. Documented exceptions:
#
#   - ST1005: the edit tool's LLM-facing error strings (capitalization
#     and trailing punctuation) are deliberate — they are tool payloads
#     returned to the model, asserted byte-for-byte by tests, and
#     capitalization aids model legibility.
#   - ST1020/ST1023: ported comment/redundancy style.
#
# All SA* (correctness) checks remain enabled.
checks = ["all", "-ST1005", "-ST1020", "-ST1023"]
